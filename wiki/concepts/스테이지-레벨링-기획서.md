---
type: concept
tags: [system, 스테이지, 레벨링, 무림인-키우기]
created: 2026-07-26
updated: 2026-07-27
sources: []
---

# 무림인 키우기 — 챕터 1 스테이지 레벨링 기획서

## 1. 스테이지 번호 체계

### 1.1 표기 규칙
- 스테이지는 `대스테이지-소스테이지` 형식으로 표기한다. 예: `1-1`, `3-7`, `10-10`.
- 대스테이지는 1~10 (챕터 1 기준), 각 대스테이지 내 소스테이지는 1~10으로 고정.
- 챕터 1 전체 소스테이지 수 = 10(대스테이지) × 10(소스테이지) = **100개**.
- 스테이지 진행은 선형(1-1 → 1-2 → ... → 1-10 → 2-1 → ... → 10-10)이며 스킵 불가, 이전 소스테이지 클리어 시 다음 소스테이지 잠금 해제.

### 1.2 보스 스테이지 표기
- 각 대스테이지의 마지막 소스테이지(X-10)는 보스 스테이지로 고정.
- UI/데이터 상으로는 별도 태그를 부여해 구분: 예) `stageType: "NORMAL" | "BOSS"`.
- 표기 예시: `1-10 (보스: 혈랑채 두목)`, `10-10 (보스: 혈교 하남지단주 척망)` 등 보스명을 병기(정확한 매핑은 1.3절 참고).
- 보스 스테이지는 스테이지 선택 UI에서 아이콘/프레임 색상을 다르게 하여 시각적으로 구분(예: 붉은 테두리).

### 1.3 대스테이지 보스 ↔ 스토리보드 사건 매핑 (2026-07-27 확정)

이전 버전에서 스테이지 문서(하오문 두목/사천당가 가주)와 UX 목업(종남파 장로 위지패)이 [[스토리보드-기획서]]의 실제 챕터1 사건과 서로 다른 이름을 썼다. 아래 표가 유일한 정본이며, UX 목업(`ux-시나리오-목업.html`, SVG 4종)에 남아있는 "위지패" 등은 자리표시자 예시였을 뿐 정식 명칭이 아니다.

| 대스테이지 | 보스 | 스토리보드 근거 |
|---|---|---|
| 1 | 혈랑채 두목 | 1장: 산 아래를 위협하는 도적 떼 |
| 2 | 흑시장 조직 두목 | 2장: 진기 갈취 흑시장 |
| 3 | 팽가 소공자 | 3장: 팽가 소공자와의 첫 비무 |
| 4 | 사공(邪功) 사용자 | 4장: 비무대회 중 사공을 쓰는 자 |
| 5 | 혈교 살수 두목 | 5장: 청운문 원로 의문사, 혈교 살수 첫 감지 |
| 6 | 마두(부패 지현 결탁) | 6장: 관부와 결탁한 마두 |
| 7 | 심마(心魔) — 내면전 | 7장: 주화입마 직전, 소주천 완성, 심마와 대면 (통상 몬스터가 아닌 특수 연출 보스전) |
| 8 | 혈교 선봉대장 | 8장: 정파 연합과 혈교 선봉대 격돌 |
| 9 | 청운문 배신자(사형) | 9장: 사문 습격, 배신자 정체 발각 |
| 10 | 혈교 하남지단주 척망 | 10장: 챕터1 최종보스 (기 확정) |

대7 보스전은 유일하게 일반 몬스터가 아니라 "내면의 자신"과의 특수 전투 연출이므로, 구현 시 별도 스크립트 이벤트(일반 HP/ATK 스케일링 대신 고정 연출 시퀀스)로 처리하는 것을 권장한다.

## 2. 난이도 곡선

### 2.1 기본 스케일링 공식 (소스테이지 내부, 같은 대스테이지 안에서)
대스테이지 `N`, 소스테이지 `S` (S = 1~9, 일반 스테이지 기준)일 때 몬스터 기본 스펙:

```
MonsterHP(N, S)   = BaseHP(N) × (1 + 0.15 × (S - 1))
MonsterATK(N, S)  = BaseATK(N) × (1 + 0.12 × (S - 1))
MonsterDEF(N, S)  = BaseDEF(N) × (1 + 0.10 × (S - 1))
```
→ 같은 대스테이지 안에서는 소스테이지 1개 증가할 때마다 체력 +15%, 공격력 +12%, 방어력 +10% 수준의 완만한 상승.

### 2.2 대스테이지 간 스케일링 (BaseHP/BaseATK/BaseDEF 자체의 증가)
대스테이지가 바뀔 때는 소스테이지 내 상승폭보다 훨씬 큰 배율을 적용한다.

```
BaseHP(N)  = BaseHP(1)  × 1.8^(N - 1)
BaseATK(N) = BaseATK(1) × 1.6^(N - 1)
BaseDEF(N) = BaseDEF(1) × 1.5^(N - 1)
```

즉 `1-10 → 2-1` 전환 시 체력 기준 약 1.8배, 공격력 약 1.6배 점프. 반면 `1-1 → 1-2`처럼 대스테이지 내부 전환은 체력 15%, 공격력 12% 수준 상승에 그침 → **대스테이지 전환의 난이도 상승폭이 소스테이지 내 상승폭보다 명확히 크게** 설계됨.

### 2.3 배율 표 (BaseHP 기준, N=1일 때 100 가정)

| 대스테이지 N | BaseHP(N) | 전 대스테이지 대비 배율 |
|---|---|---|
| 1 | 100 | - |
| 2 | 180 | ×1.8 |
| 3 | 324 | ×1.8 |
| 4 | 583 | ×1.8 |
| 5 | 1,050 | ×1.8 |
| 6 | 1,889 | ×1.8 |
| 7 | 3,401 | ×1.8 |
| 8 | 6,122 | ×1.8 |
| 9 | 11,019 | ×1.8 |
| 10 | 19,834 | ×1.8 |

참고로 소스테이지 내부 최대 상승폭(S=1→S=9)은 (1+0.15×8) = 2.2배 수준으로, 대스테이지 1회 전환(1.8배)과 비슷하거나 작은 수준을 유지해 "대스테이지가 바뀔 때 훅 어려워진다"는 체감을 준다.

## 3. 보스 스테이지 규칙

X-10 보스 스테이지는 동일 대스테이지의 일반 몬스터(X-9) 대비 다음과 같이 차별화한다.

| 항목 | 일반 스테이지(X-9) 대비 배율/차이 |
|---|---|
| HP | ×3.0 |
| 공격력 | ×2.0 |
| 방어력 | ×1.5 |
| 처치 시 경험치 | ×5.0 |
| 처치 시 재화(골드/영약 등) | ×5.0 |
| 확정 드랍 | 해당 대스테이지 전용 장비/무공 비급 1종 확정 드랍 (일반 스테이지는 확률 드랍만) |
| 특수 기믹 | 보스 전용 패턴 최소 1개 보유 (예: 체력 50% 이하 시 광폭화로 공격속도 +30%, 또는 일정 시간마다 광역 공격 예고 후 시전) |
| 재도전 제한 | 없음(방치형 특성상 자동 재시전), 단 최초 클리어 시에만 확정 보상 지급, 반복 클리어는 파밍용 보상만 |

기믹은 챕터1 내에서도 대스테이지별로 최소 1개씩 다르게 부여하여(예: 1-10은 일반 광폭화, 5-10은 방어막 페이즈, 10-10은 광폭화+방어막 복합) 챕터 후반으로 갈수록 기믹이 중첩/복잡해지는 구조를 권장한다.

## 4. 플레이어 성장 곡선과의 매칭

플레이어가 각 대스테이지에 "무리 없이" 도전할 시점에 기대되는 성장 구간(예시):

| 대스테이지 | 예상 플레이어 레벨 | 예상 전투력(전투력 지수, 1-1 시작을 100 기준) | 예상 무공 숙련도 단계 |
|---|---|---|---|
| 1 | 1 ~ 10 | 100 ~ 250 | 입문(1성~2성) |
| 2 | 10 ~ 18 | 250 ~ 450 | 2성~3성 |
| 3 | 18 ~ 26 | 450 ~ 800 | 3성~4성 |
| 4 | 26 ~ 34 | 800 ~ 1,440 | 4성~5성 |
| 5 | 34 ~ 42 | 1,440 ~ 2,600 | 5성~6성 (1차 전직 권장 구간) |
| 6 | 42 ~ 50 | 2,600 ~ 4,650 | 6성~7성 |
| 7 | 50 ~ 58 | 4,650 ~ 8,370 | 7성~8성 |
| 8 | 58 ~ 66 | 8,370 ~ 15,070 | 8성~9성 |
| 9 | 66 ~ 74 | 15,070 ~ 27,120 | 9성~10성 (2차 전직 권장 구간) |
| 10 | 74 ~ 82 | 27,120 ~ 48,820 | 10성~극성(챕터1 최종 숙련도) |

**매칭 방식 설명**: 몬스터 BaseHP/ATK가 대스테이지당 약 1.8배/1.6배로 오르므로, 플레이어 전투력도 대스테이지당 약 1.8배 내외로 성장해야 "약간의 노력(사냥/장비 파밍/무공 숙련도 업)으로 다음 대스테이지에 도전 가능"한 밸런스가 유지된다. 소스테이지 내부(15%~22% 완만한 상승)는 방치 사냥만으로 자연 성장(레벨업, 장비 강화)으로 따라잡히도록 설계하고, 대스테이지 전환 시점(보스 클리어 직후)에는 전직/무공 각성/신규 장비 세트 등 "구조적 성장 이벤트"가 발생하도록 유도해 난이도 점프를 상쇄시킨다.

**"무공 숙련도 단계(성급)" ↔ [[무공-시스템]] 실측치 환산표 (2026-07-27 추가):** 위 표의 "N성"은 서사/UI용 요약 등급이며, 실제 데이터는 무공-시스템의 보드 Lv/대성 진행도다. 환산 기준(삼재검법 1보 기준): 1성=기수식만 습득, 2~4성=1차 노드 3종이 각각 Lv10/25/40 진행, 5성=1차 노드 전부 Lv50(대성), 6~8성=2차 노드 진행 및 신규 보드(회선장법 등) 병행 습득, 9~10성=2차 노드 대성, 극성=캡스톤(삼재합일) 대성 + 챕터1 마지막 보드(폭뢰도법)까지 습득 완료.

## 5. 재화/경험치 획득 곡선

소스테이지 `X-S` 클리어 시 획득량은 몬스터 스펙과 유사한 곡선을 따르되, 재화는 약간 더 완만하게(파밍 반복을 유도하기 위해) 설계한다.

```
EXP(N, S)  = BaseEXP(1) × 1.8^(N-1) × (1 + 0.15 × (S-1))
Gold(N, S) = BaseGold(1) × 1.6^(N-1) × (1 + 0.12 × (S-1))
Chi(N, S)  = BaseChi(1) × 1.8^(N-1) × (1 + 0.15 × (S-1))
```

- 경험치는 몬스터 HP 스케일(1.8배/대스테이지)과 동일한 곡선을 따라, 상위 대스테이지일수록 레벨업에 필요한 경험치 총량 증가분을 보상 증가분이 어느 정도 상쇄하도록 함.
- 재화(골드 등 강화 재료)는 1.6배로 다소 완만하게 하여, 상위 스테이지 재화가 "인플레이션"되지 않고 강화 비용 곡선과 맞물리게 함.
- **내공(Chi) 곡선 (2026-07-27 신규 정의):** 이전 버전엔 EXP(캐릭터 레벨업용)와 Gold(=전錢, 장구 강화용)만 있고 무공/문파무공 강화에 쓰는 내공 곡선이 없었다. 내공은 EXP와 같은 1.8배 곡선을 쓴다(둘 다 "성장을 가속하는" 자원이라는 성격이 같으므로). `BaseChi(1) = 80`(예시 anchor) 기준, 대1 소스테이지 첫 클리어 ≈ 80 내공, 대10 보스 클리어(×5.0) ≈ 80×19,834×2.2×5.0 ≈ 1,745만 내공. [[무공-시스템]]의 5개 보드 대성 총합(≈1,073M)과 비교하면 챕터1 전체를 통틀어 스테이지 클리어만으로는 모자라고, 문파 기여도 경유·일괄 파밍 반복이 필요하다는 뜻 — 이는 의도된 것으로, 5개 보드를 챕터1 안에서 전부 대성할 필요는 없고 순차 해금 자체가 진행 지표 역할을 한다.
- 보스 스테이지(X-10)는 위 3장 규칙대로 EXP/Gold 모두 ×5.0 배율 적용.
- 추가로 "첫 클리어 보너스"(스테이지당 1회 한정 대량 보상)를 별도로 얹어, 신규 스테이지 최초 돌파 시 성장 가속을 준다: `FirstClearBonus = EXP(N,S) × 3` (일반), `EXP(N,10) × 2` (보스, 이미 ×5가 적용되어 있으므로 추가 배율은 낮게).

## 6. 챕터 1 총괄 요약표

| 대스테이지 | 대략적 난이도 배율 (전 대스테이지 대비) | 보스 특징 한줄 | 예상 플레이어 성장 구간 |
|---|---|---|---|
| 1 | 기준(×1.0) | 잡배 두목급, 기믹 없음. 튜토리얼 성격의 첫 관문 | Lv.1~10, 전투력 100~250 |
| 2 | ×1.8 | 체력 50% 이하 시 광폭화(공속 +30%) | Lv.10~18, 전투력 250~450 |
| 3 | ×1.8 | 일정 주기 광역 예고 공격 추가 | Lv.18~26, 전투력 450~800 |
| 4 | ×1.8 | 광폭화 + 광역 공격 복합, 방어력 비중 증가 | Lv.26~34, 전투력 800~1,440 |
| 5 | ×1.8 | 방어막 페이즈(일정 체력 구간 무적/보호막) 등장, 1차 전직 관문 성격 | Lv.34~42, 전투력 1,440~2,600 |
| 6 | ×1.8 | 방어막 + 광폭화 복합, 속성/상태이상 저항 부여 | Lv.42~50, 전투력 2,600~4,650 |
| 7 | ×1.8 | 소환수/졸개 소환 기믹 추가 | Lv.50~58, 전투력 4,650~8,370 |
| 8 | ×1.8 | 소환 + 광역 + 방어막 복합 기믹 | Lv.58~66, 전투력 8,370~15,070 |
| 9 | ×1.8 | 페이즈 전환형 보스(체력 구간별 패턴 변경), 2차 전직 관문 성격 | Lv.66~74, 전투력 15,070~27,120 |
| 10 | ×1.8 | 챕터1 최종보스: 전 기믹(광폭화/방어막/소환/페이즈전환) 총집합, 확정 보상 최상급 | Lv.74~82, 전투력 27,120~48,820 |

## 7. 전투력 지수 합산 공식 (2026-07-27 신규 정의)

이 문서 4장/6장에 등장하는 "전투력 지수"는 지금까지 여러 시스템 문서에 흩어진 % 버프를 하나의 숫자로 합치는 값이다. 아래를 프로젝트 전체의 유일한 공식으로 채택한다.

```
전투력 = BaseStat_총합 × (1 + Σ가산버프)

BaseStat_총합 = 캐릭터 기본 스탯(HP/ATK/DEF 등) + 장구 슬롯 8개의 BaseStat 합
              ([[장구-시스템]] BaseStat = ItemLevel × Coeff × SlotWeight × GradeMultiplier)

Σ가산버프 = 무공 노드 효과(%) + 장구 강화 단계 보너스(%) + 문파 특전(%) + 환골탈태 영구버프(%)
```

**스태킹 규칙(가산 vs 곱연산 확정):** 위 네 항목( 무공/장구강화/문파특전/환골탈태 )은 전부 **하나의 가산(합산) 버킷**에 더한 뒤, 그 합계를 BaseStat_총합에 딱 한 번 곱한다. 항목별로 따로 곱연산을 적용하지 않는다 — 이렇게 해야 시스템이 늘어나도(향후 챕터 추가 시) 버프가 지수적으로 폭주하지 않는다.

예시: BaseStat_총합 1,000, 무공버프 +40%, 장구강화 +25%, 문파특전 +3%, 환골탈태 1회(+15%) → 전투력 = 1,000 × (1+0.40+0.25+0.03+0.15) = 1,000 × 1.83 = **1,830**.

### 7-1. 캐릭터 레벨 → BaseStat 곡선 + 대10 종점 검산 (2026-07-27 신규 정의)

지금까지 BaseStat_총합에 실측값이 있는 항이 하나도 없어 4장/6장의 "전투력 100→48,820" 표를 역산할 수 없었다. 아래 값들로 채워 대략 검산한다(전부 초기 밸런싱용 예시값, 실측 필요):

```
PlayerBaseStat_char(Lv) = 100 × 1.052^(Lv-1)   [캐릭터 자체 기본 스탯, 장구 제외]
```

Lv1=100, Lv82≈6,050. 여기에 [[장구-시스템]]의 `BaseStat_gear_총합`(대략 Lv82 시점 등급 상품~절품 평균 장착 가정 시 ≈3,800~4,000)을 더하면 `BaseStat_총합(Lv82) ≈ 10,000`.

가산버프 합계(대10, 이론상 상당 부분 진행된 상태 가정): 무공(5보드 대성 시 최대 145%, 현실적 도달치 ~100%) + 장구강화(슬롯 평균 +10~15단계 가정 ~150%) + 문파특전(Lv30 기준 +12%) + 환골탈태(3~5회, +45~75%) ≈ 100+150+12+60 = **322%**.

`전투력(대10) ≈ 10,000 × (1+3.22) = 42,200` — 6장 표의 대10 구간(27,120~48,820) 범위 안에 들어온다. 완벽히 정합되진 않지만(초기 예시값이라 당연), 공식이 실제로 "계산 가능"함을 보인다. 실측 밸런싱 시 위 예시 계수(캐릭터 곡선 지수 1.052, 무공/장구/문파 %표)를 실제 플레이 데이터로 재조정할 것.

### 7-2. 4장 표의 대1→대2 성장률 이상치 확인

4장 표에서 대1→대2 구간만 전투력이 ×2.5(100→250)로, 나머지 전 구간(×1.78~1.81)과 다르다는 지적이 있었다. **의도된 값으로 확정** — 대1은 튜토리얼 구간이라 신규 유저가 빠르게 첫 성장을 체감하도록 의도적으로 낮은 시작치(100)에서 크게 도약시킨 것이다. 나머지 구간은 몬스터 스케일링(×1.8/대스테이지)과 정합되도록 유지.

## 8. 일일 플레이 시간 가정치 (2026-07-27 신규 정의)

지금까지 여러 문서(예: [[문파-시스템]]의 "일 평균 200 기여도", [[환골탈태-시스템]]의 재도전 소요시간표)가 각자 다른 암묵적 세션 길이를 전제로 산정되어 있었다. 아래를 프로젝트 전체의 공통 기준값으로 채택한다.

```
기준 세션: 하루 3회 접속 × 세션당 평균 10분 = 일일 활성 플레이 30분
```

이 30분은 방치형 특성상 "화면을 계속 보고 있는 시간"이 아니라 "앱을 열고 확인/재화 소비/재출발시키는 능동 조작 시간"을 의미하며, 자동전투 자체는 앱이 열려 있는 동안 계속 진행된다. 이 문서(스테이지 파밍 속도)와 무공-시스템(강화 소요), 장구-시스템(강화 시도 횟수), 문파-시스템(기여도 75일), 환골탈태-시스템(재도전 소요) 전부 이 "일일 30분" 가정 위에서 산정된 것으로 간주한다. 추후 실측 데이터로 조정 시 이 값만 바꾸면 다른 문서의 산정치도 비례 재계산 가능하도록, 각 문서의 시간 기반 수치는 이 상수를 기준으로 도출된 것임을 명시해둔다.

---

이상으로 챕터 1(대스테이지 1~10, 소스테이지 1-1~10-10 총 100개)의 스테이지 레벨링 기획을 정리하였다. 실제 밸런싱 시에는 위 배율(1.8/1.6/1.5, 0.15/0.12/0.10)을 실제 플레이 데이터(방치 사냥 속도, 강화 비용 곡선 등)에 맞춰 미세 조정하는 것을 권장한다.
