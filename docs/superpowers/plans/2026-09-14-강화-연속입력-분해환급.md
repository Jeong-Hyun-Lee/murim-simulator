# 강화 연속입력 · 분해 골드환급 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** (1) 장비 강화 버튼을 꾹 누르면 연속 강화되게 하고, (2) 강화된 장비 분해 시 강화에 쓴 골드 일부를 환급한다.

**Architecture:** 둘 다 `GearPanel.tsx`(UI) + `store.ts`(로직) 두 파일 안에서 끝나는 작은 변경. 새 컴포넌트·훅 없음 — 기존 `useHoldRepeat` 훅과 `enhanceCost` 함수를 재사용.

**Tech Stack:** React + TypeScript, 기존 패턴 그대로.

## Global Constraints

- 설계 문서: `docs/superpowers/specs/2026-09-14-강화-연속입력-분해환급-design.md`
- 환급률 50% 확정(사용자 선택).
- 강화석 환급(등급 기준) 로직은 건드리지 않는다 — 골드 환급을 추가할 뿐.
- `enhance()`의 골드 부족 시 토스트 early-return은 그대로 둔다 — zustand는 동일 문자열 재설정 시 구독자에게 불필요한 리렌더를 일으키지 않으므로(값 동일 시 selector가 변경 없음으로 판단), 꾹 누르기로 반복 호출돼도 시각적 스팸이 없다. `disabled`를 골드까지 포함하도록 넓히지 않는다(버튼이 회색으로 바뀌어 클릭 시 토스트로 알려주던 기존 동작이 사라지는 걸 막기 위함).
- 커밋 전 `npm run lint`(위반 시 `npm run lint:fix`) 실행.
- 커밋 메시지: 한글, Conventional Commits, 완결형 어미 금지, Co-authored-by 트레일러 금지.

---

### Task 1: 강화 버튼 꾹 누르기 연속 강화

**Files:**
- Modify: `src/components/panels/GearPanel.tsx`

**Interfaces:**
- Consumes: `useHoldRepeat(action: () => void, disabled: boolean)` (기존, `src/hooks/useHoldRepeat.ts`, `GongPanel.tsx`에서 이미 쓰는 패턴 그대로).
- Produces: 없음(단일 태스크 내 UI 변경, 다른 태스크가 참조하지 않음).

- [x] **Step 1: `useHoldRepeat` import 추가**

`GearPanel.tsx` 최상단 import 목록(현재 1-22번 줄)에 추가:

```tsx
import { useHoldRepeat } from '../../hooks/useHoldRepeat';
```

- [x] **Step 2: `EnhanceBlock`에서 훅 연결**

`EnhanceBlock` 컴포넌트 안, `enhance` 함수 정의(현재 111-126번 줄) 바로 다음에 추가:

```tsx
  const hold = useHoldRepeat(enhance, disabled);
```

- [x] **Step 3: 버튼에 훅 핸들러 연결**

현재 163-165번 줄:

```tsx
        <button type="button" className="btn btn-primary" disabled={disabled} onClick={enhance}>
          +{targetLevel} 강화하기
        </button>
```

를 아래로 교체:

```tsx
        <button
          type="button"
          className="btn btn-primary hold-btn"
          disabled={disabled}
          // eslint-disable-next-line react/jsx-props-no-spreading
          {...hold}
        >
          +{targetLevel} 강화하기
        </button>
```

(`hold-btn` 클래스와 spread 방식은 `GongPanel.tsx`의 동일 패턴을 그대로 따른 것 — `useHoldRepeat`이 반환하는 onPointerDown/onPointerMove/onPointerUp/onPointerLeave/onPointerCancel/onClick/onContextMenu를 개별 나열하면 훅 캡슐화가 깨진다는 그쪽 주석과 같은 이유.)

- [x] **Step 4: 린트 실행**

Run: `npm run lint`
Expected: 에러 없음.

- [x] **Step 5: 브라우저에서 수동 확인**

Run: `npm run dev`
- 골드/강화석을 충분히 보유한 상태에서 장비 슬롯 시트를 열고 강화 버튼을 마우스로 누른 채 400ms 이상 유지.
- 여러 단계 연속으로 강화 시도(성공/실패 텍스트가 계속 바뀜)되는지 확인.
- +15(최대) 도달 시 자동으로 멈추고 "최대 강화(+15) 달성" 문구가 뜨는지 확인.
- 강화석이 바닥나면 자동으로 멈추는지 확인(짧은 클릭은 여전히 1회만 실행되는지도 확인).

- [ ] **Step 6: 커밋**

```bash
git add src/components/panels/GearPanel.tsx
git commit -m "feat: 장비 강화 버튼 꾹 누르기 연속 강화 지원"
```

---

### Task 2: 분해 시 강화 골드 환급

**Files:**
- Modify: `src/game/store.ts`
- Modify: `src/components/panels/GearPanel.tsx`

**Interfaces:**
- Consumes: `enhanceCost(currentLevel: number): number` (기존, `gearData.ts`, `store.ts`에 이미 import돼 있음), `GearItem` 타입(기존).
- Produces: `enhanceGoldRefund(items: GearItem[]): number` — `store.ts`에서 `export const`로 새로 정의, `GearPanel.tsx`에서 import해 분해 확인 시트에 표시. `disassembleItems` 액션의 `gold` 증가량 계산에도 내부적으로 사용.

- [x] **Step 1: `enhanceGoldRefund` 함수 추가**

`store.ts`의 `disassembleStoneYield` 정의(현재 94-97번 줄) 바로 다음에 추가:

```ts
// 강화 시도 이력(성공/실패 횟수, 실제 소모 골드)은 저장하지 않으므로, "전부 성공했다고 가정한
// 누적 강화 비용"의 50%를 근사 환급액으로 쓴다. enhanceCost(i)는 i단계→i+1단계 강화 1회 시도 비용.
const ENHANCE_REFUND_RATE = 0.5;
export const enhanceGoldRefund = (items: GearItem[]): number =>
  items.reduce((sum, it) => {
    let itemCost = 0;
    for (let lv = 0; lv < it.enhanceLevel; lv += 1) itemCost += enhanceCost(lv);
    return sum + Math.round(itemCost * ENHANCE_REFUND_RATE);
  }, 0);
```

- [x] **Step 2: `disassembleItems` 액션이 골드도 환급하도록 수정**

현재 565-578번 줄:

```ts
    disassembleItems: (itemIds) => {
      const s = get();
      const idSet = new Set(itemIds);
      const toDisassemble = s.inventory.filter((it) => idSet.has(it.id));
      if (toDisassemble.length === 0) return;
      const inventory = s.inventory.filter((it) => !idSet.has(it.id));
      const stonesGained = disassembleStoneYield(toDisassemble);
      set({
        inventory,
        enhanceStones: s.enhanceStones + stonesGained,
        toastMessage: `일괄 분해: ${toDisassemble.length}개 → 강화석 +${stonesGained}`,
      });
      persist(get());
    },
```

를 아래로 교체:

```ts
    disassembleItems: (itemIds) => {
      const s = get();
      const idSet = new Set(itemIds);
      const toDisassemble = s.inventory.filter((it) => idSet.has(it.id));
      if (toDisassemble.length === 0) return;
      const inventory = s.inventory.filter((it) => !idSet.has(it.id));
      const stonesGained = disassembleStoneYield(toDisassemble);
      const goldGained = enhanceGoldRefund(toDisassemble);
      const goldSuffix = goldGained > 0 ? ` · 전 +${goldGained.toLocaleString()}` : '';
      set({
        inventory,
        enhanceStones: s.enhanceStones + stonesGained,
        gold: s.gold + goldGained,
        toastMessage: `일괄 분해: ${toDisassemble.length}개 → 강화석 +${stonesGained}${goldSuffix}`,
      });
      persist(get());
    },
```

- [x] **Step 3: `GearPanel.tsx`에서 분해 확인 시트에 환급 골드 표시**

`GearPanel.tsx` import 목록의 `disassembleStoneYield` 옆에 추가(현재 14번 줄 근처):

```tsx
  disassembleStoneYield,
  enhanceGoldRefund,
```

현재 분해 확인 `Sheet` 안의(대략 403-407번 줄, `StatList` 추가로 줄 번호가 이전보다 밀려 있을 수 있음 — `"장비 분해"` 제목의 `Sheet`를 찾아서 적용) 아래 블록:

```tsx
          <p>
            선택한 장비 <strong>{checkedItems.length}개</strong>를 분해해 강화석{' '}
            <strong>{disassembleStoneYield(checkedItems)}개</strong>를 얻습니다. 분해한 장비는
            되돌릴 수 없습니다.
          </p>
```

를 아래로 교체:

```tsx
          <p>
            선택한 장비 <strong>{checkedItems.length}개</strong>를 분해해 강화석{' '}
            <strong>{disassembleStoneYield(checkedItems)}개</strong>
            {enhanceGoldRefund(checkedItems) > 0 && (
              <>
                , 전 <strong>{enhanceGoldRefund(checkedItems).toLocaleString()}</strong>
              </>
            )}
            를 얻습니다. 분해한 장비는 되돌릴 수 없습니다.
          </p>
```

- [x] **Step 4: 린트 실행**

Run: `npm run lint`
Expected: 에러 없음.

- [x] **Step 5: 브라우저에서 수동 확인**

Run: `npm run dev`
- +0 장비만 분해: 골드 환급 문구가 안 뜨고 골드가 그대로인지 확인.
- +5 이상 강화한 장비를 분해: 확인 시트에 "전 N" 문구가 뜨고, 분해 실행 후 실제로 골드가 그만큼 느는지 확인.
- 여러 개(강화된 것 + 안 된 것 섞어서) 동시 분해 시 골드 환급이 강화된 것들만 합산되는지 확인.

- [ ] **Step 6: 커밋**

```bash
git add src/game/store.ts src/components/panels/GearPanel.tsx
git commit -m "feat: 강화 장비 분해 시 강화 골드 절반 환급"
```
