# murim-simulator

"무림인 키우기" — 현대에서 살던 사람이 무림으로 이세계 전이해 성장하는 방치형(idle) 캔버스 웹게임.

@.claude/common-guidelines.md 지침을 따른다.

## 기획 위키

`wiki/` 폴더가 이 프로젝트의 지식 위키다. 작업 전 `wiki/CLAUDE.md` 참고. 게임 기획 산출물(스토리, UX, 시스템 등)은 `wiki/concepts/`에 파일링한다.

위키 사서 역할: `wiki/` 내 산출물 추가·수정 시 기존 문서와 중복·모순 없는지 확인, 알맞은 폴더(`concepts/`, `design/`, `entities/`, `sources/`, `synthesis/`, `raw/`)에 분류, `wiki/index.md`와 `wiki/log.md` 최신 상태 유지 책임진다.

## UX팀 (기획 서브에이전트)

`agents/UX팀/`에 페르소나 3개가 있다: 스토리 담당자, UX 담당자, 스테이지 레벨링 담당자.

사용자가 "UX팀 스폰해줘" 등으로 요청하면 `ecc:team-builder` 스킬로 `agents/UX팀/` 도메인 전원(3명)을 병렬로 스폰한다. 도메인 선택 메뉴를 거치지 않고 바로 전원 선택 → 작업 내용만 확인 후 진행.

## 디자인팀 (비주얼 디자인 서브에이전트)

`agents/디자인팀/`에 페르소나 3개가 있다: 배경 디자이너(장소/환경), 에셋 디자이너(캐릭터/적/아이템/아이콘), UX 디자이너(디자인 시스템/톤앤매너/컴포넌트 비주얼).

사용자가 "디자인팀 스폰해줘" 등으로 요청하면 `ecc:team-builder` 스킬로 `agents/디자인팀/` 도메인 전원(3명)을 병렬로 스폰한다. 도메인 선택 메뉴를 거치지 않고 바로 전원 선택 → 작업 내용만 확인 후 진행.

UX팀의 `ux-담당자`(화면 구성·플로우 기획)와 디자인팀의 `UX 디자이너`(비주얼 스타일)는 역할이 다르다 — 혼동 시 사용자에게 확인.
